import { AddModalPropertiesDirective } from './add-modal-properties.directive';

describe('AddModalPropertiesDirective', () => {
  it('should create an instance', () => {
    const directive = new AddModalPropertiesDirective();
    expect(directive).toBeTruthy();
  });
});
